import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { ComponentAnatomy } from "docs/components/ComponentAnatomy";
import { Tooltip } from "@jpmorganchase/uitk-core";
import { withFlexGap } from "docs/decorators/withFlexGap";
import {
  CSSClassTable,
  CSSVariableTable,
  CharacteristicUsage,
} from "css-variable-docgen-components";
import { Default, OpenTooltip } from "./Tooltip.stories";

<Meta
  title="Documentation/Core/Tooltip"
  component={Tooltip}
  parameters={{
    viewMode: "docs",
  }}
/>

# Tooltip

A Tooltip provides guidance to the user when something requires more explanation or context, or to provide more information about an error or warning condition. A Tooltip will typically be displayed after the user’s mouse hovers over the target element for a certain amount of time.

## Default

<Canvas>
  <Story id="core-tooltip--default" />
</Canvas>

## Open Tooltip

<Canvas>
  <Story id="core-tooltip--open-tooltip" />
</Canvas>

## Tooltips with state

<Canvas>
  <Story id="core-tooltip--error-tooltip" />
  <Story id="core-tooltip--warning-tooltip" />
  <Story id="core-tooltip--success-tooltip" />
</Canvas>

# API

```
import { Tooltip } from "@jpmorganchase/uitk-core";
```

## Props

<ArgsTable of={Tooltip} />

## CSS Class

<CSSClassTable of={Tooltip} />

## Characteristics Used

<CharacteristicUsage of={Tooltip} />
